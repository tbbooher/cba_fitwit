emacs (text editor Tim is using)

Live reload (browser plugin)

audit home page for things loading so slowly

cold theme being used (themeforest.net)

** name and address for git 

// Ruby Info //
source ~/.bash_profile

bundle install

rails s  (this starts rails server)

guard (starts live reload / testing server)

// GIT INFO //
cd fitwit/
ls (list)

git pull origin master

git commit -a -m ""   (-a = all, -m = message) 

git push origin master

// mongo // 
mongod run --config /usr/local/Cellar/mongodb/2.0.1-x86_64/mongod.conf

app->views->base

turn off js in chrome for dev
Dec 2