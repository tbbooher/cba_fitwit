= simple_form_for :credit_card, :url => {:action => 'pay', :id => params[:id]} do |f|
  = f.input :first_name, input_html: { :value => @user.first_name }
  = f.input :last_name, input_html: { :value => @user.last_name }
  = f.simple_fields_for :billing_address do |ba|
    = ba.input :street_address1, label: 'Address', input_html: { :value => @user.street_address1 }
    = ba.input :street_address2, required: false, label: " Address 2 "
    = ba.input :city, input_html: { :value => @user.city }
    = ba.input :us_state, label: "State", input_html: { :value => @user.us_state }
    = ba.input :zip,input_html: { :value => @user.zip }
  = f.input :number, hint: "No dashes or spaces"
  = f.input :month, as: :date,  discard_day: true, discard_year: true
  = f.input :year, as: :date, discard_month: true, discard_day: true, start_year: Date.today.year
  = f.input :type, collection: Order::card_types
  = f.button :submit, "Submit Order", class: "big-btn"
  -#%ul
  -#  %li= desc_label(f,:first_name,'First Name:') + f.text_field(:first_name, :value => @user.first_name) |
  -#  %li= desc_label(f,:last_name,'Last Name:') + f.text_field(:last_name, :value => @user.last_name)
  -#  / begin address
  -#  %li
  -#    = desc_label(f,:address,'Billing Address:',true)
  -#    #address.address
  -#      %div
  -#        = text_field_tag('billing_address[address1]', @user.street_address1, :class => "full") + |
  -#          f.label(:street_address1,"Street Address")                                               |
  -#      %div
  -#        = text_field_tag('billing_address[address2]', @user.street_address2, :class => "full") + |
  -#          f.label(:street_address2,"Street Address (cont)")                                        |
  -#      %div
  -#        %span
  -#          / City !
  -#          = text_field_tag('billing_address[city]', @user.city, :size => '22') + |
  -#            f.label(:city, "City")                                                 |
  -#        %span
  -#          / State !
  -#          = text_field_tag('billing_address[us_state]', |
  -#            @user.us_state, :size => 2) +             |
  -#            f.label(:us_state, "State")                 |
  -#        / Zip !
  -#        %span
  -#          = text_field_tag('billing_address[zip]', @user.zip, :size => 10) + |
  -#            f.label(:zip, "Zip")                                               |
  -#  / end address
  -#  %li
  -#    = desc_label(f,:number,'Credit Card Number:')
  -#    %span
  -#      = f.text_field(:number) +                               |
  -#        label(:number, :description, "(No spaces or dashes)") |
  -#  %li
  -#    = desc_label(f,:month,'Expiration Date:')
  -#    %span
  -#      = f.text_field(:month, :size => 2) + |
  -#        label(:credit_card, :month, "MM")  |
  -#    %span
  -#      = f.text_field(:year, :size => 4) +   |
  -#        label(:credit_card, :month, "YYYY") |
  -#  %li= desc_label(f,:number,'Type:') +    |
  -#      f.select(:type, Order::card_types) |
  -#  %li= desc_label(f,:this_submit,' ',false) +    |
  -#      f.submit('Submit Payment', :class => "button") |