<%= hidden_field_tag "#{commentable.class.to_s.underscore}_id" , commentable.id.to_s %>
 <%= f.input :name %>
 <%= f.input :email  %>
 <%= f.input :comment, as: :text, input_html: {rows: 5, style: "width:400px;"} %>
 <div class="form-actions">
   <%= f.submit "Post comment", class: "btn btn-primary" %>
 </div>