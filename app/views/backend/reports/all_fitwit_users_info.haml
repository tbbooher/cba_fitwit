%h1 Reports: All FitWit Users Info

%p
  You can also view the
  = link_to 'FitWit Member Info',  backend_member_info_report_path
  report.

%p
  Get this report as a
  = link_to 'CSV', backend_users_report_path(format: :csv)

%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Location
      %th Member (T/F)
      %th Email
      %th Address
      %th Phone
      %th Camps Completed
      %th Terminated
  %tbody
  - @users.each do |u|
    %tr
      %td= link_to u.name, edit_backend_user_path(u.id)
      %td= u.location_name
      %td= u.member
      %td= u.email
      %td= u.one_line_address
      %td= u.primary_phone
      %td= u.camps_completed
      %td= u.date_membership_terminated

%hr
