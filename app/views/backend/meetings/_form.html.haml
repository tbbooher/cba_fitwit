= simple_form_for [:backend, @location, @fitness_camp, @time_slot, @meeting] do |f|
  %fieldset
    = f.input :meeting_date, :input_html => { :class => 'datepicker' }
  .form-actions
    = f.submit class: "btn btn-primary"
