%h1 Emergency Contact Information

- unless @campers.size > 0
  %p
    There are no campers registered for
    = @ts.short_title
- else
  %p
    You have
    = @campers.size
    campers.
  #camp_info
    %table.table.table-striped
      %thead
        %tr
          %th Camper
          %th Contact Name
          %th Emergency Contact Phone
          %th Relationship
          %th Phone
          %th Health Report
          %th Email
          %th Address
      %tbody
        - @campers.each do |camper|
          %tr
            %td= camper.full_name
            %td= camper.emergency_contact_name
            %td= camper.emergency_contact_phone
            %td= camper.emergency_contact_relationship
            %td= camper.primary_phone
            %td= camper.short_health_state
            %td= camper.email
            %td= camper.one_line_address
