- content_for :resource_actions do
  = render partial: 'my_fit_wit/includes/my_fit_wit_nav'

%h1 Welcome back #{@user.first_name}

- if @user.camper_since
  %p
    Glad you've been with us for #{time_ago_in_words(@user.camper_since)}.
- else
  %p
    You haven't registered. You should really register for a camp soon.

%p Please choose one of the options below to experience your personal FitWit:

%ul#main_my_fitwit
  %li= link_to image_tag('my_fit_wit/Full_Fitness-Progress.png'), :action => 'fit_wit_workout_progress'
  %li= link_to image_tag('my_fit_wit/Full_Camp-Reports.png'), :action => 'camp_fit_wit_workout_progress'
  %li= link_to image_tag('my_fit_wit/Full_Goals.png'), :action => 'my_goals'